const resolverUser = require('./resolverUser');
const resolverChurch = require('./resolverChurch');
const resolverMember = require('./resolverMember');
const resolverStatus = require('./resolverStatus');

module.exports = {
    resolverUser,
    resolverChurch,
    resolverMember,
    resolverStatus
}